{
  "name": "solidnotes",
  "private": true,
  "version": "1.2.0",
  "repository": "git@github.com:russelshane/solidnotes.git",
  "author": "russelshane <darsler@pm.me>",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn staging",
    "staging": "concurrently \"yarn api:dev\" \"yarn client:dev\"",
    "format": "prettier --write .",
    "prepare": "husky install",
    "lint": "eslint eslint packages/**/*",
    "client:dev": "yarn workspaces run client dev",
    "api:dev": "yarn workspaces run api dev"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "packages/**/*.{ts,tsx}": [
      "eslint --fix"
    ]
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.4.0",
    "@typescript-eslint/parser": "^5.4.0",
    "concurrently": "^6.4.0",
    "eslint": "^8.3.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^4.0.0",
    "husky": "^7.0.4",
    "lint-staged": "^12.1.2",
    "prettier": "^2.5.0",
    "typescript": "^4.5.2"
  }
}
